name: first-workflow

on:
  push:

jobs:
  learn:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls -la
      - run: whoami
      - run: uname -a
      - run: docker --version
      - run: python3 --version
